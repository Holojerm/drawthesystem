{
  "name": "sysdesign-prep",
  "version": "0.1.0",
  "private": true,
  "description": "System design interview practice with any AI coding agent + Excalidraw. Skills for research, scenarios, mock interviews (text/voice), diagram critique, reference solutions.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "web:install": "npm --prefix web install",
    "web": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "voice": "node scripts/voice.mjs serve",
    "demo": "node -e \"const fs=require('fs');const d='sessions/'+new Date().toISOString().slice(0,10)+'-generic-url-shortener';fs.cpSync('examples/demo-session',d,{recursive:true});console.log('created '+d)\"",
    "check": "node --check scripts/excalidraw.mjs && node --check scripts/read-excalidraw.mjs && node --check scripts/to-clipboard.mjs && node --check scripts/voice.mjs && node scripts/excalidraw.mjs examples/sample-spec.json /tmp/sdp-check.excalidraw && node scripts/read-excalidraw.mjs /tmp/sdp-check.excalidraw > /dev/null && echo ok"
  }
}
